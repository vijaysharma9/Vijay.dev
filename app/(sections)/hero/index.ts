/**
 * Route-group wrapper for the `HeroSection` (no URL segment).
 * Re-exported so you can change import paths without changing Home assembly.
 */
export { default } from '@/components/sections/HeroSection';

